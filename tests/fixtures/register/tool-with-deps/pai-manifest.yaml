name: tool-with-deps
version: "2.0.0"
type: cli+mcp
description: A tool that depends on simple-tool

provides:
  cli:
    - command: with-deps run
  mcp:
    - tool: with_deps_run

depends_on:
  - name: simple-tool
    type: cli
    version: ">=1.0.0"

reliability: 0.92
debt_score: 2
